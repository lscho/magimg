[[permission]]
identifier = "allow-image-compression"
description = "Allow the main window to prepare, preview, run, save, cancel, and release local image compression sessions."
commands.allow = [
  "compression_prepare",
  "compression_thumbnail",
  "compression_run",
  "compression_save",
  "compression_cancel",
  "compression_release",
]
