# 幻画 AI 后端接口说明

> 本文件原有 `/v1` 邮箱认证、套餐支付和同步生成契约已经废弃。

当前客户端和服务端联调以 [CLIENT_API.md](CLIENT_API.md) 为唯一接口契约，基础路径为 `/api/client/v1`。

客户端已接入的端点、旧功能差异和仍需确认的事项见
[客户端 API 接入状态与差异](client-api-integration-gaps.md)。
